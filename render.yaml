services:
  - type: web
    name: warehouse-vision-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.render.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT src.render_main:app
    autoDeploy: true
